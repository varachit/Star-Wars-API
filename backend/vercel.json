{
  "builds": [{
    "src": "starwars/wsgi.py",
    "use": "@jgtvares/py-vercel",
    "config": { "maxLambdaSize": "15mb" }
  }],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "starwars/wsgi.py"
    }
  ]
}